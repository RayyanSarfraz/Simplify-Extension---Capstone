{
  "name": "simplify-extension",
  "version": "0.0.3",
  "description": "AI Unit Test Generator (per-function) with Run buttons",
  "main": "./extension.js",
  "scripts": {
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "vscode": "^1.99.0"
  },
  "activationEvents": [
    "onCommand:simplify.runTests"
  ],
  "contributes": {
    "commands": [
      {
        "command": "simplify.runTests",
        "title": "Run Unit Tests"
      }
    ]
  },
  "devDependencies": {
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "jest": "^29.7.0"
  }
}
